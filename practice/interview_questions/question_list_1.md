Day1 assignment
Kindly solve these questions using Java8 Streams

Filter even numbers from a list. Convert a list of strings to uppercase. 
Find the first element that starts with a given letter. 
Sort a list of integers. 
Count strings with length greater than 5. 
Remove nulls from a list using stream. 
Get the maximum element from a list. 
Concatenate a list of strings. Find if any element matches a condition (e.g., is any string empty?). 
Create a list of squared numbers. 
Group a list of words by their first letter. 
Count the number of occurrences of each word. 
Find the longest word in a list. Partition numbers into even and odd. 
Join a list of strings with commas. 
Get the average of a list of integers. 
Find duplicate elements in a list using streams. 
Convert List<String> to Map<String, Integer> (word → length). 
Get top 3 longest words from a list. 
Flatten a list of lists using flatMap(). 
Group employees by department and find highest salary in each. Find the most frequent element in a list. 
Custom Collector to count vowel and consonant words. Implement pagination (skip & limit) using streams. 
Find all anagrams in a list of strings. 
Parallel stream to process a large data set and measure performance. 
Create a nested map: Department → Role → List<Employee>. Find the top N most frequent words from a text. Use reducing() collector to concatenate strings with custom separator. 
Build your own custom Collector to mimic joining() with custom rules (e.g., trimming, ignoring nulls, adding index) 

